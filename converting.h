#ifndef _CONVERTING_H_ // Ensures this header is included only once 
#define _CONVERTING_H_

#include <stdio.h>   // Include standard input-output library
#include <string.h>  // Include string manipulation library
#include <stdlib.h>  // Include standard library for memory allocation, conversion, etc.

void converting(void); // Function declaration for the "converting" function

#endif // End of header guard
